{
    "comment": "Names tagged @public in the source that no guide describes yet. Each entry needs a reason, and an entry that is no longer needed FAILS the check — so this list can only shrink. Generated from the first run of the audience check on 2026-09-13; see API-SURFACE-REVIEW.md §8 and its plan, stage D.",
    "undocumented": {
        "BASELINE_PLAYER_VERSION": "versioning API — docs/format/README.md#versioning covers the feature, not the symbol",
        "PLAYER_WIRE_STEPS": "versioning API — docs/format/README.md#versioning covers the feature, not the symbol",
        "PLAYER_WIRE_VERSION": "versioning API — docs/format/README.md#versioning covers the feature, not the symbol",
        "PX_RATE_REJECTED": "the time contract (API review §3) — shared by the players, exposed for parity",
        "PX_TRANSFORM_PART_KEYS": "the transform part keys, in the reference only — the editor re-exports it",
        "PX_TRIGGER_DEFAULTS": "engine/trigger resolution — the guides document the document fields, not the resolver",
        "PlayerConversionResult": "versioning API — docs/format/README.md#versioning covers the feature, not the symbol",
        "PxAnimationDefinition": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "PxAnimatorCallbacks": "options/props type — the guides document its MEMBERS in a table; the type name itself is not shown",
        "PxAnimatorConfig": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "PxAnimatorConfigMergeResult": "the result type of `mergeAnimatorConfig`, documented with it in API-SCHEMA.md",
        "PxAnimatorConfigPatch": "the playback-override merge — the guides document the `timeline` option it implements",
        "PxAnimatorHandle": "options/props type — the guides document its MEMBERS in a table; the type name itself is not shown",
        "PxAttrValueSchema": "schema value — SCHEMA.md documents the shape it validates, never the value by name; §26/stage D: is it really public?",
        "PxBasicAnimatorAPI": "options/props type — the guides document its MEMBERS in a table; the type name itself is not shown",
        "PxBezierPath": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "PxBezierPathSchema": "schema value — SCHEMA.md documents the shape it validates, never the value by name; §26/stage D: is it really public?",
        "PxBinding": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "PxControlMode": "the control-mode rule the components share (API review §1/§7) — a guide would be about the props, not the resolver",
        "PxControlProps": "the control-mode rule the components share (API review §1/§7) — a guide would be about the props, not the resolver",
        "PxDefs": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "PxDiagnostic": "the diagnostics channel — the guides describe onWarn/onError/muteWarn/muteError by prop, not the types",
        "PxDiagnosticKind": "the diagnostics channel — the guides describe onWarn/onError/muteWarn/muteError by prop, not the types",
        "PxDiagnostics": "the diagnostics channel — the guides describe onWarn/onError/muteWarn/muteError by prop, not the types",
        "PxDiagnosticsConfig": "the diagnostics channel — the guides describe onWarn/onError/muteWarn/muteError by prop, not the types",
        "PxEffects": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "PxElementAnimation": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "PxElementAnimationSchema": "schema value — SCHEMA.md documents the shape it validates, never the value by name; §26/stage D: is it really public?",
        "PxEngineCallbacks": "public in the reference but never written up — decide in stage D of API-SURFACE-REVIEW.md",
        "PxGlyph": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "PxGlyphFont": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "PxGradientStopSchema": "schema value — SCHEMA.md documents the shape it validates, never the value by name; §26/stage D: is it really public?",
        "PxInfer": "the schema toolkit — used to build ON the format; only API-SCHEMA.md covers it",
        "PxKeyframe": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "PxKeyframeValueSchema": "schema value — SCHEMA.md documents the shape it validates, never the value by name; §26/stage D: is it really public?",
        "PxLoop": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "PxNode": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "PxNodeBase": "schema value — SCHEMA.md documents the shape it validates, never the value by name; §26/stage D: is it really public?",
        "PxPlaybackOverrideProps": "options/props type — the guides document its MEMBERS in a table; the type name itself is not shown",
        "PxPrerenderedOptions": "options/props type — the guides document its MEMBERS in a table; the type name itself is not shown",
        "PxRetimeEffectSchema": "schema value — SCHEMA.md documents the shape it validates, never the value by name; §26/stage D: is it really public?",
        "PxSchema": "the schema toolkit — used to build ON the format; only API-SCHEMA.md covers it",
        "PxSchemaDesc": "the schema toolkit — used to build ON the format; only API-SCHEMA.md covers it",
        "PxScroll": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "PxScrollRangePoint": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "PxScrollRangePointSchema": "schema value — SCHEMA.md documents the shape it validates, never the value by name; §26/stage D: is it really public?",
        "PxScrollRangeSchema": "schema value — SCHEMA.md documents the shape it validates, never the value by name; §26/stage D: is it really public?",
        "PxScrollSchema": "schema value — SCHEMA.md documents the shape it validates, never the value by name; §26/stage D: is it really public?",
        "PxSvgNode": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "PxSvgNodeExtra": "schema value — SCHEMA.md documents the shape it validates, never the value by name; §26/stage D: is it really public?",
        "PxTagAnimatorOptions": "options/props type — the guides document its MEMBERS in a table; the type name itself is not shown",
        "PxTimeline": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "PxTimelinePatch": "the playback-override merge — the guides document the `timeline` option it implements",
        "PxTransformParts": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "PxTransformValue": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "PxTransformValueSchema": "schema value — SCHEMA.md documents the shape it validates, never the value by name",
        "PxTrigger": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "PxTriggerSchema": "schema value — SCHEMA.md documents the shape it validates, never the value by name; §26/stage D: is it really public?",
        "RemoveIndex": "the schema toolkit — used to build ON the format; only API-SCHEMA.md covers it",
        "Vec2": "wire type — SCHEMA.md documents the shape; the name is only how TypeScript spells it",
        "WIRE_VERSION_KEY": "versioning API — docs/format/README.md#versioning covers the feature, not the symbol",
        "WireConversionConfig": "versioning API — docs/format/README.md#versioning covers the feature, not the symbol",
        "WireStepKind": "versioning API — docs/format/README.md#versioning covers the feature, not the symbol",
        "WireVersion": "versioning API — docs/format/README.md#versioning covers the feature, not the symbol",
        "WireVersionStep": "versioning API — docs/format/README.md#versioning covers the feature, not the symbol",
        "applyAnimatorConfig": "the playback-override merge — the guides document the `timeline` option it implements",
        "applyWireSteps": "versioning API — docs/format/README.md#versioning covers the feature, not the symbol",
        "clampSeekMs": "the time contract (API review §3) — shared by the players, exposed for parity",
        "controlModeTakesOverTrigger": "the control-mode rule the components share (API review §1/§7) — a guide would be about the props, not the resolver",
        "createRunClock": "the time contract (API review §3) — shared by the players, exposed for parity",
        "describeSchema": "schema value — SCHEMA.md documents the shape it validates, never the value by name; §26/stage D: is it really public?",
        "diagnoseDocument": "document tooling — listed in the reference, never written up in a guide",
        "flattenAnimatorTimeline": "document tooling — listed in the reference, never written up in a guide",
        "foldTimelineOverride": "the playback-override merge — the guides document the `timeline` option it implements",
        "isNativeForced": "engine/trigger resolution — the guides document the document fields, not the resolver",
        "isValidPlaybackRate": "the time contract (API review §3) — shared by the players, exposed for parity",
        "mayUseNativeScrollTimeline": "engine/trigger resolution — the guides document the document fields, not the resolver",
        "mergeAnimatorConfig": "the playback-override merge — API-SCHEMA.md documents its signature; the guides document the `timeline` option it implements",
        "nestAnimatorTimeline": "document tooling — listed in the reference, never written up in a guide",
        "progressSpanMs": "the time contract (API review §3) — shared by the players, exposed for parity",
        "progressToTimeMs": "the time contract (API review §3) — shared by the players, exposed for parity",
        "renderNode": "document tooling — listed in the reference, never written up in a guide",
        "resolveControlMode": "the control-mode rule the components share (API review §1/§7) — a guide would be about the props, not the resolver",
        "resolveTimelineEngine": "engine/trigger resolution — the guides document the document fields, not the resolver",
        "resolveTrigger": "engine/trigger resolution — the guides document the document fields, not the resolver",
        "schemaKeys": "the schema toolkit — used to build ON the format; only API-SCHEMA.md covers it",
        "seekCeilingMs": "the time contract (API review §3) — shared by the players, exposed for parity",
        "timeToProgress": "the time contract (API review §3) — shared by the players, exposed for parity"
    }
}
